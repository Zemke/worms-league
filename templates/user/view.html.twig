{% extends 'base.html.twig' %}

{% block title %}Report{% endblock %}

{% block body %}

<table>
    <thead>
        <tr>
            <th>Opponent</th>
            <th colspan="3">Score</th>
        </tr>
    </thead>
    <tbody>
        {% for g in games %}
            <tr>
                <td>
                    {{ g.opp.username }}
                </td>
                <td class="monospace text-right">
                    {{ g.lost }}
                </td>
                <td class="monospace p0">
                    –
                </td>
                <td class="monospace text-right">
                    {{ g.won }}
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ dump(games) }}
{{ dump(user) }}

{% endblock %}

