{% extends 'base.html.twig' %}

{% block title %}Password Forgotten{% endblock %}

{% block body %}
    <div class="text-center">
        {{ form_start(forgottenForm) }}
            {{ form_row(forgottenForm.email, {label: 'Enter your email address to reset your password'}) }}
            <button type="submit" class="btn">Reset</button>
        {{ form_end(forgottenForm) }}
    </div>
{% endblock %}

