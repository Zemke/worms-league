{% extends 'base.html.twig' %}

{% block title %}Reset Password{% endblock %}

{% block body %}
    <div class="text-center">
        {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword, {label: 'Your new password'}) }}
            {{ form_row(resetForm.activationKey) }}
            {{ form_row(resetForm.reset) }}
        {{ form_end(resetForm) }}
    </div>
{% endblock %}

